This project use:

- component:

1. doing-task-form.component
2. doing-task-list.component
3. doing-task.component
4. app.component

- directive built in:
1. *ngIf
2. *ngFor

- directive custom:
1. hovering (on selected.directive)

- pipe built in:
1. short Date pipe

- service:
1. doing-task.service

- routing:
1. app.routing

- backend:

1. mock-xhr-backend
